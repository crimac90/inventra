# Variables del frontend. Se copia como .env y no se sube al repositorio.
# Vite solo expone al navegador las variables que empiezan por VITE_.

# Direccion base de la API del backend.
VITE_API_URL=http://127.0.0.1:8000/api
